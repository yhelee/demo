定义数组存储三部汽车对象
汽车的属性：品牌，价格，颜色
创建三个汽车对象，数据通过键盘录入进来，并把数据存入到数组中

        //键盘录入
        //第一套体系：
        //nextInt();接收整数
        //nextDouble();接收小数
        //next();接收字符串
        //遇到空格，制表符，回车就停止接收，这些符号后面的数据就不会接收了

        //第二套体系：
        //netxLine();接收字符串
        //可以接收空格，制表符，遇到回车才停止接收数据

        //键盘录入的两套体系不能混用
        //弊端：先用nextInt再用nextLine会导致下面的nextLine接收不到数据